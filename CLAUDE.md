# portfolio-site — shulgin.is-a.dev

Брутализм-портфолио Александра Шульгина: Vite + React 19 + TS, CSS Modules, Motion, длинный скролл, RU/EN. Чёрный `#0b0b0c` + один коралл `#ee4e4e`, 1px рамки, прямые углы. 🔄**Редизайн 10.09.2026 по аудиту семи ролей** (память `project_portfolio_audit_20260910`): первый экран с именем и ролью, «Работы» аккордеоном, хроника с датами, сцена «Пульт», дочерние страницы в бренде, счётчик визитов, машинный слой генерируется. Ниже — только инварианты и мины; история решений в памяти.

## Структура главной (10.09.2026)
`App.tsx`: Topbar → Minimap → Hero(#intro) → Projects(#works) → Websites(#websites) → LabTeaser(#lab) → Films(#films) → Principles(#method) → Releases(#releases) → Contact(#contact) → AsciiCoda → Marquee → Footer → StickyHire. Секций «Приложения» и «Стек» больше НЕТ: приложения (`data/apps.ts`) входят в аккордеон «Работ», стек показывается чипами «через работы» (уникальные теги проектов). `Applications.tsx`, `Stack.tsx`, `ProjectCard.tsx`, `lib/scramble.ts`, `lib/useLenis.ts` удалены — не возвращать. `lib/kinetic.ts` ВОЗВРАЩЁН 12.09 по просьбе Саши (см. SectionHead).
- **Шапка:** nav = works · lab(/lab/) · proof(/proof/) · method · contact; scrollspy только по якорям `intro/works/method/contact`. Справа `Заказать` (залит кораллом, → `store/prosto/`, единственный сильный элемент шапки) + RU/EN + бургер ≤620px. ⚠️«Магазин» и «Связаться» из шапки сняты сознательно (13 целей → 7): магазин живёт в подвале и последним пунктом мобильного листа, связь — пункт nav.
- **Hero:** eyebrow = имя · роль · GMT+10 · доступность (`hero_eyebrow`); h1 три строки (`hero_l1..l3`), Glitch только один раз при монтировании и по hover на h1 (`Glitch.module.css`, класс `.play`), в покое текст чистый; две двери (`hero_cta1` → #works, `hero_cta2` → prosto); три карточки-доказательства `hc1..hc3` (третья зависит от языка: RU → /check/, EN → /curated-claude-code/); ряд статистики, где «систем в проде» = `liveSystems.length` из `data/live.ts`, под каждой цифрой строка провенанса `prov.*`. Водяная «25» и стикер MODEL_CODING удалены.
- **«Пульт» (`HeroObject.tsx`):** canvas в правой колонке = карта живых систем из `data/live.ts` (узлы = liveNodes, рёбра = liveEdges, три кластера trading/products/sites, детерминированные позиции без Math.random), импульсы по рёбрам, «дежурный»-призрак ходит по рёбрам, наведение → подпись (HTML-слой), клик → `location.hash = node.anchor` (`#work-<id>` / `#site-<id>` / `#flagship-*`). ⚠️Геометрия проекции прежняя и выверена: `R=min(w,h)*0.42`, `focal=620`, `cx=w*0.5`, `cy=h*0.42`, mask-image по краям — не менять. `Minimap.tsx` — фиксированная 120×120 (≥920px), появляется, когда #intro ушёл из вьюпорта. 🔴`live.ts` — единственный источник и для сцены, и для цифры «систем в проде»: добавил/закрыл систему → правь его (закрытые системы в него не входят, они остаются в «Работах» со статусом `closed`). Только публичные имена, никаких адресов и методов.
- **«Работы» (`Projects.tsx`):** сначала `FlagshipSystem` (id `flagship-trading`, флагман), затем `FormalMath` (id `flagship-math`, DeepMind, подан как «мой код принят в репозиторий Google DeepMind»), у обоих строка провенанса (`fmath_prov`/`flag_prov`); дальше `WorksList.tsx` — аккордеон `<details id="work-<id>">` в трёх группах (prod/open/pet → клиентские → закрытые) из `projects` + `apps`; хук `lib/useHashOpen.ts` открывает строку по хэшу и на `hashchange` (им же пользуется `Websites`: `site-<id>`); чипы стека под списком открывают строки с тегом.
- **Websites:** превью `shot` (файлы `public/sites/*.webp`, 1200×1500, показываем верх через object-position: top). **LabTeaser:** три iframe техник `/lab/techniques/<slug>/index.html` (torchere, dot-matrix, cursor-spotlight) с `loading="lazy"`, ссылки «открыть» (permalink) и «исходник» (GitHub blob). **Films:** первые 3 + «Показать все». **Principles:** без контурных 01–04, метка `p.tag`. **Releases:** 9 записей с абсолютными датами (`when` = месяц год), относительных меток нет — новая запись обязана нести дату.
- **SectionHead:** scramble снят; kinetic ВОЗВРАЩЁН 12.09 (Саша: «нравилась реакция заголовков на курсор») — буквы h2 оборачиваются в span, вес плывёт по оси wght (latin 780→900, cyr 600→700) и буква уходит от курсора (профиль «производная гауссианы», до ~5 px). Требует variable-шрифтов из `main.tsx`; НЕ вешать на hero h1 (там Glitch, спаны конфликтуют). Водяная цифра = индикатор прогресса секции (два слоя, коралловый заливается через `animation-timeline: view()` под `@supports`, без JS). Это единственный повторяющийся мотив секций. **Reveal:** 0.5 с / 16px, amount 0.12. Микро-механика hover одна на сайт: `.hoverline` в `global.css` (рамка → коралл 140 мс + линия сверху).
- **Marquee:** порядок ITEMS обновлён (торговля не первой), скорость слушает скролл через `getAnimations()[0].playbackRate`; пасхалка «42» и тост — НЕ удалять. **StickyHire:** только ≤620px, после ухода hero.
- **Lenis снят** (`scroll.ts` → нативный `scrollIntoView`, `html{scroll-padding-top:64px}`). Слои фона: `.glow` + `.gridlines` + `.dotgrid` (точки 24px, `--dot`) + `.grain` (зерно 4%, z `--z-grain`) — `.dotgrid`/`.grain` были сняты 10.09 и ВОЗВРАЩЕНЫ 12.09 по просьбе Саши («с точкой было лучше»); не снимать снова без его слова.
- **Шрифты в `main.tsx`:** статические `big-shoulders-display` 700/900, `oswald` 600/700, `jetbrains-mono` 400/600/700, `inter` 400/500/600; плюс `@fontsource-variable/big-shoulders-display/wght.css` и `@fontsource-variable/oswald/wght.css` — ТОЛЬКО для кинетики заголовков (грузятся по unicode-range, latin ≈35 КБ). `permanent-marker` удалён. Display weight 800 в статике нигде не использовать (нет файла).

## Счётчик визитов и события (10.09.2026)
На главной — модуль `src/lib/hit.ts` (`initHit()` из `main.tsx`; инлайн в `index.html` не используется: исполнитель его не вставил, живой замер 10.09 показал 0 запросов, модуль надёжнее), на статических страницах public/* — тот же инлайн-скрипт в `<head>`: `sendBeacon` на `https://backend-test.45-82-95-142.nip.io:8443/hit` с `p=<путь>&l=<язык>&r=<хост реферера>&q=<query>`; `e=depth&d=<%>` на pagehide; `e=cta&d=<имя>` по клику на любой элемент с `data-cta` (делегирование на document). Endpoint — nginx на S1 (`qsw-license`, лог `/var/log/nginx/site-hits.log`, IP сознательно не пишется). Имена событий: `header-hire/lab/proof`, `hero-works/prosto/lab/proof/third`, `work-link-<id>`, `lab-open-<slug>`, `lab-src-<slug>`, `contact-*`, `footer-*`, `sticky-hire`, `check-*`, `proof-*`. Новая кнопка → `data-cta`, скрипт не трогать. Сводка: «покажи визиты» → awk/python по логу (см. память prosto).

## Машинный слой — ГЕНЕРИРУЕТСЯ (10.09.2026)
🔴`public/llms.txt` и `public/llms-full.txt` руками НЕ править: их пишет `scripts/gen-llms.ts` из `src/data/*` + `src/i18n/dict.ts` (`npm run gen:llms` = tsc в `.gen/` + node; запускается сам через `prebuild`). Статические абзацы про страницы (/lab/, /proof/, /check/, /store/, curated) живут внутри генератора. Встроенный гейт валит сборку, если в EN-тексте есть длинное тире, «senior», «EIP-712», «$6/year», «IShu». Сгенерированные файлы коммитятся (репо читают люди и агенты). `index.html` head: title/description/OG на EN, `og:image` = `/cover.png?v=20260910` (1200×630, ≤300 КБ; исходник `og.html` не в репо — перерисовывать по образцу: чёрный фон, сетка, две строки display, три числа). `cover3.png` удалён.

## Дочерние страницы в бренде (10.09.2026)
Общий слой `public/brand/brand.css` + `public/brand/fonts/*.woff2` (копии из @fontsource: Big Shoulders 700/900 latin, Oswald 700 cyr+lat, JetBrains Mono 400/600, Inter 400/500/600) — подключается на `/proof/`, `/check/`, `/curated-claude-code/`, `/privacy/` и в публичной сборке `/lab/`. Компонент шапки `.brand-bar` (логотип SHULGIN.IS-A. · crumb раздела · RU/EN где есть · `Заказать ↗`). Новая статическая страница → подключать `brand.css` и `.brand-bar`, шрифты не дублировать. 🆕12.09: `brand.css` несёт фон главной без разметки (`html::before` свечение+точки, `body::before` линии, `body::after` зерно) — подстраницы продолжают главную («один дом»); /lab/ переопределяет в `design-showcase/tools/site/public.css` (только точки+линии, без коралла и зерна — 13 карточек в своих цветах). Магазин prosto на бумаге сознательно, туда не тянуть.
- `/privacy/` — «Данные и приватность» (RU + EN на одной странице): сайт не собирает ПД, счётчик без IP, cookies нет. Появилась, потому что `/check/` на нашей же главной находил «нет политики». Ссылка из подвала (`foot_nav.privacy`). При появлении любой формы — сначала править её.
- `/check/`: источник по-прежнему `~/Projects/tools/site-check-worker/page/index.html` (плейсхолдер `__API_URL__`), копия здесь; на странице список из 6 реальных проверок воркера, «Пример результата» = снимок живой проверки главной (не prosto: там форма-в-Telegram сознательно без чекбокса согласия), лестница «что дальше». Синхронизировать оба файла при правке.
- `/lab/`: публичная сборка design-showcase (`python3 tools/build.py --public` → `dist-public/` → `public/lab/`); бренд и коралл заданы в `tools/site/public.css` (в приватной сборке не участвует), карточки несут «Исходник» на GitHub-blob и `data-cta`, в корне `LICENSE` (MIT). Гейт перед копированием прежний (PROMPT.md/meta.json/имена приватных репо — пусто).
- `/proof/`: четыре merged-PR (#4245, #4244, #4364, #4361), секция «Как я меряю агентов». Инварианты содержания прежние (торговое и NDA не добавлять; «формализация известного результата», не «решил»).

## Раздел «Сайты» (§03 с 10.09.2026, добавлен 14.08.2026)

`Websites.tsx` + `data/websites.ts` — список сделанных сайтов между Проектами и Приложениями. Нативный `<details>`, а не сетка карточек: строк много, решение о клике человек принимает по названию и типу работы; работает с клавиатуры и без JS.

- **Ссылки проверять перед каждым релизом.** Битая ссылка в портфолио хуже отсутствующей. Проверено 14.08: `horsesfarm.ru`, `25vpn.ru`, `kratno-landing-demo`, `signal-studio-landing-demo`, `store` отдают 200 и настоящую страницу (Beget умеет отвечать 200 заглушкой — сверять `<title>`, не код).
- **Сайт без прода → без `href`**, вместо ссылки бейдж `pending` (так было у курса Татьяны до 05.09; `tanyabunina.ru` теперь с TLS и `href`). С 10.09 у строк есть `shot` (превью в `public/sites/`) и `id="site-<id>"` для открытия по хэшу; `mechta-tut` (me4tut.ru) добавлен как клиентский сайт.
- `name` двуязычное: в EN-версии русские названия выглядели чужеродно. Бренды транслитерируются (Кратно → Kratno), описательные — переводятся (Витрина товаров и услуг → Products & Services Storefront).
- Машинный слой генерируется из `websites.ts` сам (см. «Машинный слой»).

## Раздел «Ролики» (§05, добавлен 14.08.2026; с 10.09 видны первые 3 + «Показать все»)

`Films.tsx` + `data/films.ts` + `public/films/` — семь промо-роликов, собранных
нашим движком `kadr` (HTML и остановленный таймлайн GSAP → MP4).

- **Постер грузится сразу, видео только по клику** (`preload="none"`, `<video>`
  подставляется в стейте вместо `<img>`). Семь автозагрузок утопили бы мобильный
  трафик, а звук без спроса запускать нельзя.
- **Файлы живут в `public/films/`** (mp4 720p + jpg-постер, ~6 МБ на все семь).
  Исходники роликов НЕ здесь, а в `~/Projects/tools/promo-films/<имя>/`.
  Пересборка веб-копий: `~/Projects/tools/promo-films/encode_web.sh`, потом
  скопировать в `public/films/`. ⚠️Пересобирать ПОСЛЕ каждого рендера: 14.08 в
  опубликованной папке уже лежал ролик старше своего исходника.
- При вставке секции перенумерованы `§NN` в обоих языках (Приложения §05→§06 и
  далее до Связь §09), добавлен пункт `films` в `Topbar SECTIONS` и в `nav`
  обоих языков. - Поле `style` в карточке — визуальный язык ролика. У каждого он свой намеренно:
  набор, снятый одним приёмом в разных цветах, читается как шаблон.

## Статус `closed` у карточек проектов (14.08.2026)

К `Status` добавлено значение `closed` («ЗАКРЫТ» / «RETIRED», класс `.closed`,
приглушённый, без акцента) — для работ, снятых с эксплуатации, которые остаются
в портфолио как пример. Первый носитель — 25.VPN: сервис сделан нами, но снят с
продажи; ссылка на сайт убрана, из описаний вычищены «коммерческий», тарифы и
подписка.

⚠️**Ссылки проверять перед каждым релизом.** Прогон 14.08 поймал битую: репозиторий
`crypto-dashboard` больше не публичен и отдавал 404 с карточки Sector Map.
Проверка: собрать все `https?://` из `src/data/*.ts` и `public/llms*.txt`, дёрнуть
HEAD. ⚠️LinkedIn отвечает 999 или 405 на любой не-браузерный запрос, это не
поломка.

## Мобилка (проверено 29.07.2026 на 320/360/390/414)
- ⚠️**Гетч flex/grid: `min-width:auto` у flex-элемента распирает страницу.** Кейс 29.07: в `.row` (Contact) лейбл имеет `min-width:5.5rem`, а `.rowVal` с длинным email не сжимался → `.cards` 352px в родителе 323px → документ 370px при экране 360. Симптом коварный: `body{overflow-x:hidden}` (global.css:24) **прячет** боковой скролл, поэтому визуально это не «страница едет», а молча обрезанный текст справа. На iPhone-390 не воспроизводится — только ≤380px. Фикс: `@media (max-width:520px)` в `Contact.module.css` — `.row{flex-wrap:wrap}` + лейбл на всю строку + `min-width:0` на значении. Идиома проекта — `min-width:0` (уже в `Releases`/`Marquee`).
- **Как проверять:** Playwright с `is_mobile=True` + `reduced_motion="reduce"` (иначе reveal-контент `opacity:0` и замер врёт), критерий = `documentElement.scrollWidth <= clientWidth+1` И ноль текстовых узлов правее `clientWidth` вне `overflow:hidden`. ⚠️Элементы `position:fixed` и содержимое Marquee (`_track_`/`_item_`) из проверки исключать — они шире экрана намеренно.
- ⚠️`npm run preview`/localhost заворачивается Shadowrocket → Playwright запускать с `args=["--no-proxy-server","--proxy-bypass-list=<-loopback>"]`, тогда локальный `dist` через `python3 -m http.server` проверяется нормально.

## Деплой (ИЗМЕНИЛСЯ — больше НЕ git-push index.html)
- Репо `Sanexxxx777/Sanexxxx777.github.io` (**PUBLIC, приватным делать НЕЛЬЗЯ** — это и есть Pages-сайт).
- ⚠️**29.07: `~/Projects/portfolio-site` ТЕПЕРЬ настоящий git-клон** (`origin` = `Sanexxxx777.github.io`, ветка `main`) — процедура «clone→cp→push» ниже устарела, правки коммитятся прямо здесь. Сверку `git fetch` перед работой всё равно делать: репо бывает новее.
- Сборка через **GitHub Actions** (`.github/workflows/deploy.yml`): `npm install && npm run build` → артефакт `dist` → Pages. ⚠️ `npm install` НЕ `npm ci` (рассинхрон lock на кросс-платформ нативном биндинге Vite8/rolldown `@emnapi/*` → `npm ci` падает на linux CI).
- ⚠️ **Pages Source в настройках репо ДОЛЖЕН быть "GitHub Actions"**, НЕ "Deploy from branch" — иначе Actions-деплой не применится.
- `CNAME` лежит в `public/` → попадает в `dist` (домен не слетит). OG-обложка `public/cover.png` (1200×630, ≤300 КБ), старая `cover3.png` удалена 10.09.
- ⚠️**10.09.2026: сборка `main` падала на TS-ошибке** (`apps.ts` третья строка meta при типе `[Meta, Meta]`) и Actions был красным двое суток, живой сайт молча отставал. Перед push всегда `npm run build` локально; тип теперь `meta: Meta[]`.
- Откат: ветка `legacy-singlefile` хранит старый single-file `index.html`.

## `public/bg/` + `public/mechta-tut/` — клиентский заказ антикафе «Мечта Тут» (19-20.08.2026)

Статические страницы вне сборки Vite (`public/` копируется как есть), `noindex, nofollow`, из sitemap исключены, ниоткуда не залинкованы.
- 🟢**`mechta-tut/index.html` — актуальный боевой путь (с 20.08.2026), сюда все дальнейшие правки.** Василий утвердил `bg/best.html` и попросил доработать «серьёзно» — контент перестроен под два направления бизнеса: `#zaly` (аренда залов под мероприятия — день рождения/квиз/турнир/корпоратив, калькулятор зал×часы×ведущий) и `#free` (свободное посещение по минутам/дню, коллекция игр). Плюс компактный блок `#mesto` (антикафе: адрес/атмосфера/контакты) перед `#bron`.
- **`bg/` целиком заморожен** — `bg/index.html` (шесть ранних направлений) и `bg/best.html` (последний показанный клиенту снимок перед перестройкой) не трогать и не удалять, это история показа.
- ⚠️**Шрифты `mechta-tut/` self-host полностью** (`mechta-tut/fonts/`, свои `@font-face` с unicode-range cy/lat) — Unbounded/Onest/JetBrains Mono/Caveat, скопированы из `node_modules/@fontsource/*`. `bg/best.html` тянул эти же шрифты через Google Fonts CDN (открытый долг из `reference_152fz_website_mines`) — в `mechta-tut/` починено, в `bg/` сознательно НЕ трогаем (заморожен).
- ⚠️**`bg/img/wordmark.png` и `mascot-badge.png` НЕПРОЗРАЧНЫЕ** (белый/графитовый фон — на тёмной теме дают плашку). В `mechta-tut/` data-URI `WORD` с 20.08 = прозрачная версия 169×120 (сплошной коралл alpha=255, края разбелены; НЕ формулой «alpha=255−min(rgb)» — она делает коралл полупрозрачным). Файл-исходник прозрачной версии: `bg/img/wordmark-cut.png`.
- 🔴**Логотип в шапке — полный леттеринг `wordmark.png` (data-URI `WORD`), НЕ значок-вырез `mascot-cut.png` (`CUT`).** Заказчик прямо попросил: маленький M — для чата/аватарки, наверху сайта должен быть полный «Мечта Тут». `signMark` в сцене входа (неоновая вывеска) сознательно остался `CUT` — это крест/эмблема над рукописным названием, отдельный сценический элемент, не «шапка».
- **Заявка → Telegram, без сервера** (152-ФЗ): `#leadForm` собирает 3 поля в браузере, кнопка открывает `t.me/<username>?text=...`. 🔴`TG_USERNAME = "mechta_tut_bron"` в `<script>` — ЗАГЛУШКА, заменить на реальный контакт Василия при получении.
- **Черновые цифры залов/цен собраны в один комментарий** в начале `<script>` (не JS-объект — просто справочный список, расчёт берёт `data-rate` из разметки) — при получении реальных чисел от Василия менять и в этом комментарии, и в разметке `.rooms`/`.calc`. Названия залов по цветам (Красный/Синий/Зелёный/Жёлтый) — плейсхолдеры, реальных 6, у нас описаны только 4 (карточка «Ещё два зала» — честная заглушка, не выдуманы вторые два).
- Живой адрес проверять с S1 (`site-check/live_check.py`) или через браузер: с Мака Shadowrocket подменяет DNS.
- Открытые вопросы к Василию: TG-username/номер для брони, точные цвета/вместимость/цены 6 залов, доступы к хостингу `me4tut.ru` (SpaceWeb, PHP 8.4) для будущего переезда с нашего Pages.
- **Шапка (21.08):** `.design-credit` («Заказать дизайн») перенесён в разметке сразу после `.brand-lockup`, ДО `.spacer` — сидит слева, рядом с логотипом, а не справа рядом с CTA «Оставить заявку» (два ярких элемента рядом путали приоритет). Порядок: логотип → design-credit → spacer → nav → address-chip → CTA. Разделитель — `.design-credit::before` (линия 38px с градиентным затуханием, альфа .34); прежний `border-left` с альфой .14 заказчик просто не видел и решил, что разделителя нет.
- 🔴**Hero — водяной знак маскота (21.08, переделан в тот же день):** `#heroMarkGlow` (`.hero-mark-glow`) — `CUT` data-URI, `opacity:.3`, `filter:saturate(.9)`, БЕЗ blend и blur, лежит прямым ребёнком `.hero` (`right:4%;top:8%`), на ≤999px скрыт. ⚠️**Не возвращать его внутрь `.hero-art` и не включать `mix-blend-mode:screen`/`drop-shadow`:** первая версия (`opacity:.07`, screen, blur) легла поверх `players-line.webp` и дала мутное тёплое облако ~400px, съевшее контраст line-art — заказчик прочитал это как «весь сайт пожелтел, контраст рисунка неестественный». Доказано изоляцией: `display:none` на одном этом элементе — облако исчезает.
- 🔴**`color-scheme:dark` обязателен (21.08):** `<meta name="color-scheme" content="dark">` + `:root{color-scheme:dark}`. Без них браузер с принудительным тёмным режимом (Chrome/Brave auto dark mode) инвертирует наши светлые поверхности: белая грань кубика чернеет, песочная активная вкладка зала становится болотной, бежевая схема `.whole-plan` — коричневой. Заказчик читал это как «сайт пожелтел» и «чёрный кубик», хотя в обычном браузере всё верно. Воспроизводится ТОЛЬКО через CDP `Emulation.setAutoDarkModeOverride` (флаги запуска Chromium эффекта не дают) → [[reference_browser_auto_dark_mode_trap]].
- **Палитра остыла (21.08):** `--paper:#F1EDE8` (было `#F2E8DC`), кремовый в заголовках `#FAF7F3` (было `#FFF9EE`), `--lamp:#EBC9A2` (было `#EFC08A`). Ламповый золотой — ТОЛЬКО акцент (`.hero h1 em`, активная вкладка зала, ховеры): цены (`.hall-price`, `.free-tariff .price`, `.room .price`, `.whole-stats b`, `.free-tariff.stop .price`) и буллеты `.amenities li::before` с него сняты. Грань кубика `.dice .side` — настоящая белая `#FFFFFF→#E7E4DF` (была кремовая `#FDF7EA→#E4D5BB` и читалась «пожелтевшей»). ⚠️Заказчик считает тёплую гамму дефектом — новые элементы красить в `--paper`/`--sage`, лампу тратить точечно.
- **`.stage-spot` (курсор-свечение) уменьшен 21.08** — было `min(860px,84vw)×min(700px,78vw)` blur(76px), стало `min(560px,56vw)×min(460px,50vw)` blur(62px). Правило живёт в «Свет — два размытых однотонных слоя» ближе к концу `<style>` (второй `.stage-spot` блок побеждает каскадом — редактировать именно его, не первый ранний).
- **`console-perk` (PS5-плюшка) — история трёх заходов, чтобы не ходить по кругу:** (1) была отдельной карточкой с бордером/фоном и крупной консолью+двумя геймпадами анфас — заказчик: «читается неоднозначно», плюс «большой блок чёрного фона»; (2) 21.08 ужата до иконки геймпада 32px в обычном `<li>` — заказчик: «ПС5 вообще убрали»; (3) 21.08 вечер вернули иллюстрацию ~96-118px (`clamp(96px,10vw,118px)`), но пункт ОСТАЁТСЯ обычной строкой списка. 🔴Инвариант: карточку-рамку вокруг него НЕ возвращать, консоль рисовать в ракурсе три четверти (не анфас), консоль и геймпад — две НЕ пересекающиеся фигуры с зазором, иначе читается как «смятая бумага».
- **`.warm-perk .perk-icons` — орбита (21.08):** три иконки расставлены по кругу через `--orbit-start`/`--orbit-counter`/`--orbit-radius` и крутятся `perkOrbit 24s linear infinite`. ⚠️Контр-вращение (`--orbit-counter` = минус стартовый угол, и в кейфрейме −360deg) обязательно — без него чашка едет вверх дном. Отключается в `prefers-reduced-motion` (`animation:none!important`), на узких экранах свой радиус/размер.
- 🟢**Админка контента (21.08, боевая):** цены залов, тариф свободного посещения и анонсы едут не из разметки, а с нашего сервиса на S1 — `https://backend-test.45-82-95-142.nip.io:8443/mechta/api/content` (панель: тот же хост, `/mechta/admin`; код `S1:/root/mechta-admin/`, pm2 `mechta-admin`, порт 8378, токен в его `.env`). На странице — `mtApplyContent()` + `fetch` в конце `<script>`. 🔴Фолбэк обязателен и уже есть: недоступный сервис оставляет цифры из разметки, поэтому статические цены в HTML держать в актуальном состоянии, они — резерв, а не мусор. ⚠️Интеграцию НЕЛЬЗЯ проверить с локального сервера: CORS разрешает только `https://shulgin.is-a.dev`, с `127.0.0.1` фетч падает — это ожидаемо, проверять на живом URL.
- **Анонсы клуба (`#announcements`):** секция между `#mesto` и `#partner`, по умолчанию `style="display:none"`, наполняется функцией `renderAnnouncements(items)` из ответа админки (см. пункт выше); пустой список держит секцию скрытой. Карточки рендерятся через `.rooms`/`.room` (те же классы, что у блока #mesto) — новый CSS не заводить. ⚠️Локальный `announcements.json` больше НЕ читается (файл оставлен на диске) — при правках не «чинить» его, источник данных теперь один: сервис на S1.
- **Партнёрам — карточка «Инвестиции и спонсорство» (21.08):** 5-й пункт `.partner-offer-invest` в `.partner-offer`, full-width (`grid-column:1/-1`), честная заглушка без цифр — статус/бонусы за вложение обсуждаются с Василием отдельно, как и «Ещё два зала».
- **Reveal-переходы (21.08, два захода):** сначала получили `filter:blur(7px)`+`clip-path` против «грубой обрезки блоков», но заказчик прочитал это как «блоки прогружаются при пролистывании» — блюр и clip-path СНЯТЫ, остался короткий fade со сдвигом 8-12px (`.motion-ready .motion-reveal` + именные оверрайды `.night`/`.whole-rental`/`.free-tariffs`/`.warm-perk`/`.console-perk`). ⚠️Не возвращать блюр в появление: на тёмном фоне размытый прямоугольник читается как незагруженный контент.
- **Ритм страницы (21.08):** `section{padding:56px 0}`, на ≥760px — `72px` (было 76/104). Прежние 104+104 давали между секциями пустые чёрные провалы — жалоба «большой блок чёрного фона, не красиво».
- **Пагинация карусели (21.08):** при 21 игре круглые точки читались россыпью — теперь узкие засечки 8×2px, активная расширяется до 18px (`.cf-dot::after` + `.cf-dot.on::after`). Ряд работает шкалой позиции. Каскад `transitionDelay` по глубине СНЯТ — заказчик прочитал его как дёрганость, ряд едет одним движением.
- **Аудит 21.08 (`e2e_audit.py` по локальному дев-серверу):** консоль/JS чисто, DOM не растёт, форма НЕ шлёт на сервер (ожидаемо — TG deep-link), cookies/третьих сторон нет. Два открытых пункта, не исправлены намеренно: (1) на странице нет ссылки на политику обработки ПД — решение о тексте политики за Василием/Сашей, не выдумывать; (2) вес страницы ~1.3 МБ (порог скрипта 1 МБ) — драйвер, вероятно, инлайновые base64-логотипы и шрифты, не тронуто (рефактор вне сегодняшнего запроса).

### Coverflow-карусель обложек (`#free`, добавлена 20.08, чинена в тот же день)
- 3D coverflow (`.cf-shell > .cf-viewport > .cf-stage > .cf-card`) — центральная обложка крупная и плоская, соседние уходят в `rotateY`/`scale`/`opacity` по offset от центра, зациклена, тянется мышкой/пальцем/стрелками/клавиатурой/точками. Структура НАРОЧНО в три вложенных слоя: `overflow:hidden` и `perspective` разнесены по разным элементам (Safari плющит 3D-контекст, если они на одном), края — `mask-image`-фейд, не жёсткий срез.
- **Обложки — реальные фото коробок** в `img/covers/*.webp`, с официальных страниц издателей/розницы (Hobby World, Hobby Games, «Правильные игры» rightgames.ru, CGE для Codenames) — НЕ с BGG (запрещено их Terms) и не с маркетплейсов. Обработаны PIL: флуд-филл белого фона от краёв в альфа + автокроп по непрозрачному контенту (скрипт разовый, не сохранён — при повторной обработке переписать по аналогии). Исходники до обработки — `img/covers/raw/` (НЕ в гите, только локально на Маке, для повторной обработки при необходимости).
- **21.08: сначала 14 игр вместо 10, затем 21** — вторым заходом переобработаны последние семь (`activity`/`carcassonne`/`catan`/`evolution`/`imaginarium`/`svintus`/`uno`): у них был не «ободок», а целая белая прямоугольная подложка, поэтому их держали вне карусели. Рецепт чистки (скрипт разовый, не сохранён): флуд-филл от КРАЁВ холста по светлому с допуском ~26 → в альфу; затем выкинуть связные компоненты меньше 2% от самого крупного (осиротевшие куски фона: у `uno` их было 347px, у `evolution` 21px); затем автокроп по альфе, ресайз в 760px, WEBP q88. ⚠️Флуд именно от краёв, не по всему холсту — иначе съедается светлая передняя грань коробки (`evolution` почти белая). ⚠️Проверка «нет белых пикселей по периметру холста» НЕ ловит такие подложки (вокруг них прозрачные поля) — смотреть контактным листом глазами.
- ⚠️Текст `.cf-intro` больше НЕ содержит числа игр («вот те, с которых обычно начинают») — раньше требовал синхронизации с длиной `GAME_COVERS` и разошёлся бы при каждом добавлении.
- **Загрузка ленивая** — `IntersectionObserver` на `#cfShell`, фото подставляются только когда секция близко к экрану (изначально виден только CSS-градиент карточки, мгновенно). ⚠️Тестировать в headless/фоновой вкладке браузерной автоматизации бесполезно — Chrome троттлит и `requestAnimationFrame`, и `IntersectionObserver` в задних (`document.hidden`) табах; проверено программным monkey-patch rAF в этой сессии, реальный браузер (видимая вкладка) не троттлит.
- Шаг карусели `CF_STEP` — константа (202px десктоп / 138px при `max-width:480px`, увеличено 21.08 — было 174/118, карточки визуально слипались краями), синхронизирована с CSS-брейкпоинтом карточки — не разводить их разными числами.
- **Ход карусели (21.08):** `transition:transform 560ms var(--ease-cf)` (`--ease-cf:cubic-bezier(.16,.84,.24,1)`) вместо прежних 220ms — заказчик просил «плавнее». Плюс каскад: `cfLayout` ставит `transitionDelay` = `min(|offset|*26, 120)ms`, поэтому дальние коробки трогаются позже и ряд идёт волной. ⚠️Во время драга задержка принудительно `0ms` (иначе перетаскивание отстаёт от пальца) — ветка `cfDragging ? "0ms" : …`; сам transition на драге и так снят инлайном в `pointerdown`.
- Каждый шаг проходит через `cfGoTo(index)` (не только `cfStep(dir)`) — единая точка для seam-фикса (гасит transition карточкам, чей offset скачет через границу цикла), иначе клик по дальней точке пагинации даёт видимый телепорт через весь экран.

## `public/lab/` — Лаборатория приёмов (публичная витрина механик, 04.09.2026)

Сборка приватного репозитория `~/Projects/web/design-showcase` в публичном режиме: 13 живых механик, каждая открывается и работает в браузере. Входы с главной (10.09): пункт «Лаборатория» в шапке, карточка-доказательство в hero (`hc1_*`), секция-тизер `LabTeaser` с тремя iframe, ссылка в подвале.

🔴**Папка собирается, а не пишется руками.** Обновление: `cd ~/Projects/web/design-showcase && python3 tools/build.py --public`, затем скопировать `dist-public/` в `public/lab/`. Правки прямо в `public/lab/` затрутся следующей сборкой.

🔴**Что режет публичный режим и почему (`tools/build.py`, `make_ignore` + ветки `if public`):**
- `PROMPT.md` — воспроизводящие брифы, наш капитал; в публичную сборку не копируются, `llms-full.txt` (их полный дамп) не пишется вовсе.
- `meta.json` — несёт `source.path` с именами проектов; среди источников есть приватный репозиторий (`nfa-design-system`) и клиентские работы. Поле `source` также вырезается из `catalog.json` и из встроенного каталога, иначе карточка печатает «источник: <имя приватного репо>».
- Кнопка «Промпт» в карточке рисуется только при непустом `item.prompt_html` (`tools/site/index.js`) — в публичной сборке данных нет, кнопка исчезает сама. Диалог остаётся в разметке мёртвым, это нормально.

**Проверка перед публикацией** (обязательна, один прогон): `find public/lab -name PROMPT.md -o -name meta.json` пусто, и `grep -rioE "mechta-tut|nfa-design|prism-design|portfolio-site|/root/|/Users/sasha" public/lab/` пусто. Мобильные ширины — `viewport_audit.py` по `/lab/`, прошло 4/4.

⚠️Машинный слой обновляется той же правкой вручную: раздел `## Interaction lab` в `llms-full.txt`, ссылка в `llms.txt`, URL в `sitemap.xml`.

## `public/proof/` — «Доказательства» (страница верифицируемых пруфов, 04.09.2026)

Один статический двуязычный файл (RU/EN переключаются на месте, выбор в `localStorage`, автодетект по `navigator.language`). Вне сборки Vite, как `mechta-tut/`. Входы (10.09): пункт «Доказательства» в шапке, карточка hero `hc2_*`, подвал. Merged-PR теперь четыре (#4245, #4244, #4364, #4361), сверять `gh api` перед релизом.

Смысл страницы: закрыть дыру «в профиле нет ничего машинно-проверяемого» ([[project_upwork_profile_next_session]]). Поэтому туда попадает **только то, что открывается по ссылке и проверяется чужими руками**: три merged-PR в `google-deepmind/formal-conjectures`, открытые репозитории, живые сайты.

🔴**Инварианты содержания, не размывать:**
- Торговые системы, боты и NDA-работы на страницу НЕ добавлять. Про это есть отдельный абзац «чего здесь нет и почему» — он и есть позиция, а не недоработка.
- Формулировки честные: «воспроизводит известную границу», НЕ «решил задачу»; «принято в репозиторий», НЕ «работал в DeepMind».
- Перед каждым релизом прогонять ссылки (правило раздела «Сайты»); 10.09 все ссылки из `src/data` и `public` дали 200 (LinkedIn 999 — норма).

Машинный слой: раздел Evidence генерируется из `formalMath.ts`; статический абзац про страницу живёт в `scripts/gen-llms.ts`.

## `public/check/` — бесплатная проверка сайта (05.09.2026)

Статическая страница + **Cloudflare Worker** `https://site-check.site-check-worker.workers.dev` (код и инструкции — `~/Projects/tools/site-check-worker`, приватный репозиторий `Sanexxxx777/site-check-worker`). Первая, бесплатная ступень лестницы офферов: показывает ЧТО нарушено и ЧЕМ грозит, но НЕ как чинить — фиксы платные.

🔴**Почему воркер, а не наш сервер.** Адрес для проверки диктует посетитель. На S1/S2/S3 это SSRF-поверхность рядом с торговыми ботами и VPN: `http://127.0.0.1:8378/` заставил бы сервер сходить в собственный внутренний сервис. Воркер изолирован от нашей сети. **Не переносить эту проверку на наши серверы ни при каких упрощениях.**

Защита в воркере (три дыры первой версии нашло автоматическое ревью коммита, все закрыты до деплоя): только веб-порты 80/443, свой резолв через DNS-over-HTTPS с проверкой всех адресов хоста на приватность, редиректы вручную с перепроверкой каждого прыжка, единый непрозрачный ответ на любую сетевую неудачу (разные коды были оракулом портов). Лимит 20 проверок в час с адреса.

⚠️Адрес воркера зашит в `public/check/index.html` (`var API`). Источник страницы — `site-check-worker/page/index.html` с плейсхолдером `__API_URL__`; правки вносить там и копировать сюда, иначе разойдутся.
⚠️CORS воркера пускает только `https://shulgin.is-a.dev` и `localhost:8792` — при смене домена править `ALLOWED_ORIGINS` в воркере.
Машинный слой: статический абзац про страницу живёт в `scripts/gen-llms.ts`.

Проверка боем 04.09 нашла реальные долги на наших же клиентских сайтах: `me4tut.ru` — плеер YouTube (трансграничная передача), две формы с полями персональных данных без галочки согласия, нет политики; `horsesfarm.ru` — нет политики. Это известный открытый пункт по «Мечте Тут», теперь подтверждённый машиной.

## Шрифты — ГЕТЧ
- `--f-display` = `"Big Shoulders Display", "Oswald", ...`. **Big Shoulders НЕ имеет кириллицы** (только latin/latin-ext/vietnamese) → EN-заголовки идут в Big Shoulders, **RU-заголовки в Oswald** (per-glyph fallback). НЕ убирать Oswald, иначе русские заголовки отвалятся на системный фолбэк.
- Self-host через `@fontsource` (скилл design-taste-frontend запрещает `<link>` Google Fonts). Inter/JetBrains Mono кириллицу тащат.

## Контент — линия приватности (ИНВАРИАНТ)
- Витрина: **флагман-блок «Polymarket Market-Making System»** (`src/data/flagship.ts`) объединяет 6 трейдинг-модулей в одну систему. Хвастаемся РЕЗУЛЬТАТАМИ (×13, sub-50ms, казначейство, 24/7, 3.9M).
- ⚠️ **НЕ палить МЕТОД:** EIP-712, батч-постинг, GIL, CPU-isolation, точный wire. Скрыто намеренно (репо публичный).
- Трейдинг-подсистемы НЕ дублируются как отдельные карточки (они во флагмане). `src/data/projects.ts` = остальные проекты, сильное-вперёд (Content Factory первым). Статусы сверены с pm2 10.09: у снятых систем `closed` и дата снятия в meta, у клиентских «сдан заказчику» (чужой прод не проверяем) — «live» только у того, что реально работает.
- С 10.09 вечера порядок: торговля первой (решение Саши: математику ценят немногие), математика второй, но подана громко и честно: «мой код принят в репозиторий Google DeepMind»; рендерится ПОСЛЕ `<FlagshipSystem/>`. «Формальная математика» (`FormalMath.tsx` + `src/data/formalMath.ts`, переиспользует `FlagshipSystem.module.css`) — единственное, что ревьюили чужие мейнтейнеры и что проверяется за минуту. Сворачивает Lean@DeepMind + OpenEvolve в один блок (метрики + вклады со ссылками на merged-PR/репо). ⚠️**НЕ возвращать карточки `lean`/`openevolve` в `projects.ts`** — они намеренно свёрнуты сюда, иначе дубль. i18n-ключи `fmath_*` в оба языка.

## i18n
- RU = источник/дефолт. Весь текст двуязычный: статика UI в `src/i18n/dict.ts`, контент в `src/data/*` (`{ru,en}`). **Любую правку текста вносить в ОБА языка.**
- ⚠️ **Тире:** RU оставляет типографское `—` (корректно). EN — `—` ЗАПРЕЩЁН (правило design-skill), только дефис `-`.
- Переключатель `lang` в localStorage, автодетект по `navigator.language`. OG/title/desc в `<head>` статика EN (для краулеров), JS перетирает для юзеров.

## Гетчи UI (актуально на 10.09.2026)
- **Пасхалка «42»** в `Marquee.tsx` — кликабельный `button.egg` → тост `.eggHint` (`position:fixed`, т.к. `.marquee` имеет `overflow:hidden`). НЕ удалять.
- **Coral-акцент = последнее слово заголовка** автоматически: `SectionHead` сплитит title, последнее слово в `.accent`. Касается всех секций.
- **Водяная цифра секции** стоит под линией `.top` (положительный `top`), чтобы линия не перечёркивала цифру; с 10.09 она же — индикатор прогресса (см. «Структура главной»).
- **Hero-сеть НЕ режется маской, НЕ размером.** `HeroObject.module.css` `width:min(58%,820px)`, `mask-image` затухание краёв; ❌НЕ уменьшать `R`, ❌НЕ повышать `focal`, ❌НЕ full-bleed. Высота обёртки `calc(100% - 9rem)`, чтобы узлы не заезжали на ряд статистики; у `.proof` фон и `z-index:2`.
- **Резкий coral `border-top:2px solid` → мягкий `::before` edge-fading градиент** для выделенных панелей (`FlagshipSystem.panel`, `Releases.featured`). Hard 2px coral читается как алерт — не делать.
- **Строка/карточка со ссылкой:** в `WorksList` ссылка `↗` только при `p.link`; линки — на публичные репо/артефакты; приватные/клиентские/трейдинг — без link.
- **Палитра строгая:** цвета вне палитры (голубой/фиолетовый/жёлтый у бейджей) убраны 10.09; статусы = коралл для prod, остальное `--ink-dim`. Не возвращать.
- **Полировка (make-interfaces-feel-better):** `CountUp` с `tabular-nums`; `text-wrap: balance` на заголовках. ⚠️**shadows-over-borders НЕ применять** — брутализм намеренно резкий, `border: 1px solid` = часть стиля.
- **Modern Web Guidance** скилл доступен (`~/.claude/skills/modern-web-guidance/`) — звать на веб-таски за актуальными браузерными API/Baseline.

## Структура
`src/components/*` (секции + Hero/HeroObject/Minimap/Topbar/Marquee/Footer/StickyHire + хелперы Reveal/CountUp/Glitch/MagneticButton/BurstButton), `src/data/*` (контент; `live.ts` — карта живых систем), `src/i18n/*`, `src/lib/*` (scroll/scrollspy/bi/useHashOpen/ghostEngine), `src/styles/{tokens,global}.css`, `scripts/gen-llms.ts` (машинный слой), `public/brand/` (слой бренда статических страниц). CSS Modules + нативный CSS (брутализм = без UI-фреймворка).

## Run
`npm run dev` (:5173) · `npm run build` (tsc + vite) · `npm run preview`.

## `public/curated-claude-code/` — страница проекта + бейдж LaunchBuck (07.09.2026)

Статическая страница вне сборки Vite (как `proof/` и `check/`); с 10.09 на `brand.css` и `.brand-bar`, шрифты self-host из `public/brand/fonts`. Зачем отдельная страница: у OSS-репозитория не было своей посадочной, а каталог LaunchBuck требует сайт, на котором висит их бейдж.

🔴**Бейдж — условие сделки, а не украшение.** Бесплатное размещение `curated-claude-code` в каталоге даёт dofollow-ссылку только пока бейдж `launchbuck.com/badges/pill-neutral.png` со ссылкой на `launchbuck.com/p/curated-claude-code` висит на сайте из листинга. Снимем — листинг откатится в nofollow.
⚠️**Почему бейдж не в README и не в футере React.** (1) В README GitHub принудительно проставляет `rel="nofollow"` всем внешним ссылкам, верификатор это видит и отказывает прямым текстом. (2) В `Footer.tsx` бейдж рисуется только после гидратации React, а проверялка каталога читает сырой HTML без исполнения JS. Обе версии проверены и отвергнуты 07.09, обратно не переносить.
⚠️Единственный внешний ресурс страницы (всё остальное локальное). CSP на сайте нет; появится — добавить `img-src launchbuck.com`, иначе бейдж исчезнет и верификация слетит.
Машинный слой: абзац про страницу живёт в `scripts/gen-llms.ts`.
